//Work out the first ten digits of the sum of the following one-hundred 50-digit numbers.
#include <stdio.h>
#include <string.h>

int realArray[12];
//I want to place the text file places into a character array or int array so I can add up the number.
void arrayPlace(){

}

void solve(){
	//pointer to file(which we use) then 'fopen' - followed by the name of the file, and what we want to do with it - 'r' = read.
	//FILE *text = fopen("problem13.txt", "r");
	//look more into 'EOF' - end of file to see if it can be implemented into this.
	//char fgets(char *holdArray[], )
}

int main(){



	return 0;
}